# Changelog

## v0.7.3

### 更新内容

1. 新增跳过开场动画功能，默认启用，可通过 `UI.SkipIntroMovie` 配置关闭。
2. 支持《刀剑封魔录》v1.05 与《上古传说》v2.01 的 Steam、非 Steam 启动链；仅跳过开场 `begin.dhp`，不影响后续剧情视频。
3. 补充分辨率、字体渲染与开场动画相关的逆向知识库记录。

## v0.7.2

### 更新内容

1. 支持 Steam 版《刀剑封魔录》v1.05 与《上古传说》v2.01。
2. 新增官方 Steam `launcher.exe` hook 启动链，在保留 `ComeOn.dll`、`steam_api.dll` 与 Steam 认证路径的前提下注入 `plugK.dll`。
3. 启动器支持按游戏版本选择 Steam appid，并通过 `steam://rungameid/` 启动官方 launcher。
4. 新增 `PlugKLauncherHook.dll`，用于在官方 launcher 启动 `ComeOn.exe` 时完成 MOD 注入。
5. 补充 Steam 1.05 与 2.01 启动链逆向知识库记录。

## v0.7.1

### 更新内容

1. 优化 PlugK 组合快捷键输入处理，优先在游戏键盘状态刷新 Hook 中执行快捷键逻辑，减少窗口消息链、兼容层或额外窗口导致的快捷键失效问题。
2. 调整窗口子类化查找逻辑，仅在当前进程内选择游戏主窗口，并跳过视频/DirectShow 等非游戏窗口，降低误拦截窗口消息的风险。
3. 启动器注入 PlugK.dll 时校验远程 `LoadLibraryA` 返回值，避免 DLL 实际加载失败但仍继续恢复游戏主线程。

## v0.7.0

### 更新内容

1. 本版本为 v0.6.2 之后未在 GitHub Release 发布内容的汇总发布，不额外引入新的功能范围。
2. 新增自动拾取地面物品功能，支持 1.05 与 2.01；默认关闭，可使用 `Ctrl+Z` 开关，`Shift+Z` 切换拾取范围。
3. 自动拾取支持仅金钱、金钱+宝石、金钱+宝石+护身石+回复道具、全部物品四种范围，并复用原版 Z 拾取逻辑，避免影响手动 Z 和鼠标拾取。
4. (已删除)多次优化掉落倾向功能，支持 1.05 与 2.01，可按玩家缺少的物品调整掉落倾向，并将敌人独立概率调整为期望概率模型。
5. 新增 Alt+1-4 快捷释放必杀技功能，并提供配置开关；修复在 ddraw compat 中 Alt+1-4 无法使用的问题。
6. 地面物品名称显示改为长按快捷键临时显示，减少长期显示对 GDI/DDRAW 渲染性能的影响；移除长期显示配置项和切换快捷键。
7. 新增 `Ctrl+X` 拆分背包中叠加物品的功能。
8. 支持显示敌人血量。
9. 修复九转和武神丹等高级物品进入回复快捷栏的问题。
10. 修正组合快捷键与游戏逻辑按键重复生效的问题，并优化组合快捷键冲突处理路径，降低模拟器/兼容层中的额外开销。
11. 优化自动拾取提示策略，保留开关状态和拾取范围提示，关闭逐物品获得提示以减少大量拾取时的游戏内文字输出开销。
12. 移除配置页面中的掉落倾向优化入口，默认关闭该功能。
13. 新增 `docs/reverse_kb/` 逆向知识库，记录 Hook、函数地址、运行时结构、数据表、版本差异与调试验证结论。
14. 调整发布打包流程，GitHub Release 从 `CHANGELOG.md` 中按 tag 名提取对应章节作为发布说明。
15. 替换 MinHook 依赖为随仓库提交的源码形式，减少对本地二进制库文件的依赖。

## v0.6.6

### 更新内容

1. 更新项目说明和版本信息。
2. 补充 1.05 调试模式相关逆向知识库记录。

## v0.6.5.7

### 更新内容

1. 暂时关闭自动拾取成功后的逐物品获得提示，减少大量拾取时的游戏内文字输出开销。
2. 自动拾取开关状态和拾取范围切换提示保留。
3. 优化组合快捷键冲突处理路径，降低按键检测在模拟器/兼容层中的额外开销。

## v0.6.5.5

### 更新内容

1. 自动拾取成功触发原版拾取逻辑时，新增游戏内提示：`[自动拾取] 获得：物品名 数量 N`。
2. 提示支持 1.05 与 2.01，会从地面物品运行时记录读取物品名和数量。

## v0.6.5.4

### 更新内容

1. 新增自动拾取地面物品功能，支持 1.05 与 2.01。
2. 自动拾取默认关闭，`Ctrl+Z` 开关，`Shift+Z` 切换拾取范围。
3. 自动拾取支持四种范围：仅金钱、金钱+宝石、金钱+宝石+护身石+回复道具、全部物品。
4. 自动拾取复用原版 Z 拾取逻辑，并在自动触发上下文中按物品类型过滤，避免影响手动 Z 和鼠标拾取。
5. 完成 1.05 自动拾取动态调研，确认 Z 拾取入口、地面物品拾取入口、对象槽表和地面物品字段。

## v0.6.5

### 更新内容

1. 移除配置页面中的掉落倾向优化入口，默认关闭该功能。
2. 移除长期显示地面物品名称的配置项和切换快捷键，仅保留按住快捷键临时显示地面物品名称。
3. 支持 1.05 与 2.01 掉落物品宝石的字体颜色。
4. 修复不显示血量的问题。
5. 修复九转和武神丹高级物品进入回复快捷栏。
6. 增加 `Ctrl+X` 拆分背包中叠加物品的功能。
7. 修正组合快捷键和游戏中逻辑按键重复生效问题。

## v0.6.3.9

### 修复问题

1. 修复无法在 ddraw compat 中使用 Alt+1-4 的问题。

## v0.6.3.8

### 更新内容

1. 掉落优化修改：增加敌人独立概率修改为期望概率，即不掉落时逐步提高掉落概率。

## v0.6.3.7

### 更新内容

1. 地面物品名称默认改为长按快捷键显示，减少长期显示对 GDI/DDRAW 渲染性能的影响。
2. 配置中新增“长按显示物品名称”快捷键，默认按键为 ` 键。
3. 配置中新增 Alt+1-4 快捷释放必杀技开关，可在 Player 分组中关闭。

## v0.6.3.6

### 更新内容

1. 支持物品掉落倾斜功能，偏向玩家缺少的物品。
2. 支持 Alt+1-4 释放必杀技。

## v0.6.2

### 修复问题

1. 开启叠加之后，回复类物品放入快捷栏，宝石直接进入背包，避免物品混乱。

## v0.6.1

### 更新内容

1. 增加显示敌人血量功能。

## v0.6.0

### 更新内容

1. 增加重置技能点功能，快捷键为 `Ctrl+Backspace`。

## v0.5.1.1

### 修复问题

1. 修正未开启扩展储物项时，整理物品出现数据异常的问题。

## v0.5.1

### 更新内容

1. 增加自定义单格存储物品数量上限。

## v0.5.0

### 更新内容

1. 更新程序：修正说明。

## v0.4.2.1

### 更新内容

1. 更新程序图标。

## v0.4.2

### 修复问题

1. 修正正传扩展背包功能导致继承创建存档时游戏崩溃的问题。

## v0.4.1

### 修复问题

1. 修正游戏中 plugK 提示文字乱码问题。

## v0.4.0

### 更新内容

1. 合并 loader 和 config 程序为全新的 `PlugKLauncher.exe`，在一个程序中完成配置和启动。
2. 支持启动原版游戏。
3. 支持启动 plugK 模式的游戏。
4. 支持修改配置；默认没有配置时，自动生成新版本配置内容。
5. 支持在桌面创建原版启动快捷方式和 MOD 版启动快捷方式，免去重复打开 Launcher 程序。
6. 新增支持真正的战斗、英雄无悔难度的通关存档继承。

## v0.2.9

### 更新内容

1. 重要：使用对应版本的 `plugKConfig.exe` 修改配置。
2. 增加物品自动叠加功能，游戏中可随时关闭，快捷键和宝石叠加保持相同，默认为 `Ctrl+'`。
3. 支持显示地面上的物品名称，开关快捷键为 `Ctrl+]`。
4. 解决 v0.2.8 版本正传无法进入游戏的问题。

### 注意事项

1. 关闭叠加后扔出叠加宝石为散开状态，属性随机。
2. 若想扔出后属性不变，可以在叠加状态扔出，关闭叠加状态后再捡回。

## v0.2.8

### 更新内容

1. 重要：本次更新后 `plugK.ini` 与之前版本不兼容，请使用 `plugKConfig.exe` 重新生成并保存。
2. 增加开启宝石叠加功能，游戏中可随时关闭，默认切换快捷键为 `Ctrl+'`。
3. 优化 `plugKConfig.exe` 的 UI 显示，支持配置快捷键，支持预设分辨率。

### 注意事项

1. 关闭叠加后扔出叠加宝石为散开状态，属性随机。
2. 不要在一个区域扔出太多叠加宝石，可能导致游戏崩溃。

## v0.2.7

### 更新内容

1. 尝试修正自动填充物品功能可能带来的数据丢失问题。
2. 一键整理功能针对已经开启叠加的物品可以自动合并。

### 注意事项

1. 自动填充物品功能仍处于实验中，如果发现异常，可使用配置器关闭。
2. 宝石整理没有比较价格和等级，可能将极品属性宝石合并到同类低属性的一组上。

## v0.2.6

### 更新内容

1. 增加 `plugKConfig.exe`，支持界面开启功能和配置快捷键。
2. 默认没有配置文件时，打开 `plugKConfig.exe` 后会生成 `plugK.ini`。

### 注意事项

1. 快捷键需要和游戏中的按键避开；例如设置 `Ctrl+B` 后，游戏中仍会打开或关闭背包。
2. 配置文件和以前版本不兼容，建议新创建配置文件。

## v0.2.5

### 更新内容

1. 增加合成叠加物品时扣减数量 1，避免叠加物品全部消失。

### 注意事项

1. 游戏中按格子判断基础宝石合成条件，例如需要至少在三个格子有少阴珠才能合成。
2. 功能仍处于试验阶段，可能出现异常或数据丢失。
3. 解压文件到游戏目录下，使用 `plugKLoader.exe` 启动游戏。
4. `plugK.ini` 中默认分辨率为 960x720，所有功能启动。

### 快捷键

1. `Ctrl+<` 切换储物箱。
2. `Ctrl+>` 切换背包。
3. `Ctrl+[` 整理储物箱。
4. `Ctrl+\` 整理背包。
5. `Ctrl+/` 仅整理背包当前页。

## v0.2.4

### 更新内容

1. 增加快捷键 `Ctrl+/`，仅整理背包当前页面，避免部分任务道具、避火珠之类的物品被移动到后续页面。

## v0.2.3

### 更新内容

1. 默认开启禁止技能屏幕晃动效果，可在配置文件中关闭。

## v0.2.2

### 更新内容

1. 支持自己镶嵌宝石。

## v0.2.1

### 更新内容

1. 背包实验功能：在显示背包页满的时候，物品可以自动进入扩展页背包存储；如果扩展页也满了，物品掉落在地上。
2. 背包与储物箱一键整理功能支持可见页和不可见页联动整理，例如药品自动挪到第一页。
3. 删除宝石叠加功能，因为存在无法解决的游戏程序崩溃问题。
4. 快捷键机制修改成注册快捷键，理论上减少 CPU 占用问题。
5. 启动机制改为使用 `plugKLoader.exe` 启动。
6. 移除提示音，改为使用游戏系统左侧文字展示区域。
7. 背包总共 2 页，储物箱总共 2 页。

### 注意事项

1. 该版本不兼容 v0.3 系列的 9 页背包与储物箱，不要直接读取 v0.3 存档，否则会导致背包和储物箱数据丢失；请使用新存档开始游戏。
