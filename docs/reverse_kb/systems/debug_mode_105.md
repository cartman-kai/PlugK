# 1.05 调试模式

本文件只记录 `1.05` 版本程序中已经确认或初步确认的调试模式信息。`2.01` 版本尚未调研，不应直接套用本文中的虚拟地址。

## 入口与开启方式

来源：IDA 静态分析。

文本输入框提交后会进入 `sub_4C0670`。该函数会读取输入框内容，并检查首字符是否为反引号 `` ` ``。

开启调试模式的输入为：

```text
`tianxiawudi
```

注意事项：

- 反引号 `` ` `` 必须存在。
- 反引号会先被剥离，再比较后续文本。
- 比较使用大小写不敏感逻辑，因此 `tianxiawudi` 与 `Tianxiawudi` 等价。
- 首次匹配成功后设置全局调试开关 `dword_548644 = 1`。
- 调试开关打开后，后续以反引号开头的输入会作为调试命令交给 `sub_458410` 处理。

`sub_4C0670` 中的关键行为：

| 地址/函数 | 行为 |
| --- | --- |
| `sub_4B35D0(158)` | 获取文本输入框对象 |
| `sub_519192(96)` | 消耗或移除反引号字符 |
| `dword_548644` | 1.05 调试模式全局开关 |
| `aTianxiawudi` | 开启调试模式的口令字符串 |
| `sub_458410` | 调试命令/脚本动作解释器 |

## 命令解析规则

来源：IDA 静态分析，尚未动态验证全部命令。

`sub_458410` 是 1.05 版本的主要调试命令解释器。当前观察到的解析规则：

- 先按 `*` 分割一行中的多条命令。
- 每条命令再按空格分割参数。
- 命令名使用大小写不敏感比较。

因此调试输入大致形态为：

```text
`AddItem 参数1 参数2
`AddItem 参数1 参数2 * AddExp 参数1
```

参数数量和语义需要按每个命令分支继续确认。

## 已提取的动作命令名

来源：IDA 静态分析。以下命令名来自 `sub_458410` 附近的字符串比较分支。未标注含义的命令只确认了命令名，具体参数和效果仍待确认。

| 命令 | 初步含义 |
| --- | --- |
| `Pct` | 待确认 |
| `Trans` | 将指定对象或角色传送到指定坐标，参数待确认 |
| `TransToPlayer` | 将指定对象或角色传送到玩家位置，参数待确认 |
| `Summon` | 召唤或生成对象，参数待确认 |
| `Acquire` | 待确认，可能与获得对象、物品或状态有关 |
| `AddExp` | 给当前角色增加经验，参数待确认 |
| `RemItem` | 移除物品，参数待确认 |
| `OutText` | 输出文本或调试文本，参数待确认 |
| `Miss` | 待确认 |
| `talk` | 对话相关，参数待确认 |
| `AddItem` | 添加物品，参数待确认 |
| `SetPortal` | 传送门相关状态设置 |
| `SetTimer` | 计时器设置 |
| `ReSetScn` | 场景重置相关 |
| `SetVSFlag` | VS/战斗标志设置 |
| `Say` | 角色说话或文本播放 |
| `3DEffect` | 3D 特效播放 |
| `UseMethod` | 调用对象方法或脚本方法，参数待确认 |
| `TalkFlag` | 对话标志设置 |
| `SF` | 待确认，可能是 SetFlag 简写 |
| `SetPropToObj` | 将属性或道具设置到对象上，参数待确认 |
| `ResetAllObj` | 重置所有对象 |
| `ResetObj` | 重置指定对象 |
| `SetBldStatus` | 设置建筑对象状态 |
| `SetBldAni` | 设置建筑对象动画 |
| `SetLockTextID` | 设置锁定提示文本 ID |
| `SetExitTextID` | 设置出口提示文本 ID |
| `SetExit` | 设置出口状态 |
| `AddEquipNum` | 增加装备数量或计数，参数待确认 |
| `AddWeaponNum` | 增加武器数量或计数，参数待确认 |
| `ShowJM` | 显示界面或章节画面，具体待确认 |
| `SetMerchantItem` | 设置商人商品 |
| `AddTransPortal` | 添加传送门 |
| `ChangeAct` | 切换 Act/章节 |
| `ShowActForeword` | 显示章节前言 |
| `PlayMp3` | 播放 MP3 |
| `PlaySound` | 播放音效 |
| `SetFlagTable` | 设置标志表 |
| `ReLoadSound` | 重新加载声音 |
| `UseSkill` | 使用技能，参数待确认 |
| `SetGroup` | 设置阵营或分组，参数待确认 |
| `MaxNu` | 怒气或相关属性最大值，待确认 |
| `HR` | 待确认 |
| `All` | 待确认 |
| `DP` | 待确认 |
| `MaxHP` | 最大生命相关，参数待确认 |
| `AddFellow` | 添加同伴 |
| `ScreenEffect` | 屏幕特效 |

## 条件命令

来源：IDA 静态分析。

`sub_457860` 是条件判断类命令解析器，更像脚本触发条件，不一定能作为玩家输入的调试动作直接执行。

| 命令 | 初步含义 |
| --- | --- |
| `IsProp` | 判断道具或属性 |
| `IsPropInBody` | 判断身体/装备中的道具或属性 |
| `IsSeePlayer` | 判断是否看见玩家 |
| `IsBlack` | 待确认 |
| `IsSurFaceObjNum` | 判断地表对象数量 |
| `IsArrvie` | 到达判断，游戏字符串拼写如此 |
| `IsArrvieRole` | 角色到达判断，游戏字符串拼写如此 |
| `IsScn` | 场景判断 |
| `IsScnIn` | 进入场景判断 |
| `IsScnOut` | 离开场景判断 |
| `IsFlag` | 标志判断 |
| `IsDead` | 死亡判断 |
| `IsVsFlag` | VS/战斗标志判断 |
| `IsTimer` | 计时器判断 |
| `IsLevel` | 等级判断 |
| `IsHitBox` | 碰撞盒判断 |
| `IsBldStatus` | 建筑状态判断 |

## 调试快捷键

来源：IDA 静态分析。以下为 1.05 中 `dword_548644` 开启后可达的键盘逻辑，具体玩家可见效果需要动态验证。

键盘状态数组按 Win32 virtual-key 偏移访问，当前可确认的偏移关系：

| 键 | VK |
| --- | ---: |
| `Tab` | `0x09` |
| `Enter` | `0x0D` |
| `Shift` | `0x10` |
| `Ctrl` | `0x11` |
| `Alt` | `0x12` |
| `Esc` | `0x1B` |
| `Space` | `0x20` |
| `1-6` | `0x31-0x36` |
| `A-Z` | `0x41-0x5A` |
| 小键盘 `1/2` | `0x61/0x62` |
| 小键盘 `+/-` | `0x6B/0x6D` |
| `F1-F8` | `0x70-0x77` |
| `F10` | `0x79` |
| `F11` | `0x7A` |

### `sub_4748C0`

| 快捷键 | 初步行为 |
| --- | --- |
| 方向键 | 调试移动或方向控制，方向值可见 `1/3/5/7` |
| `Shift` | 改变移动速度，观察到 `0x10` 与 `0x40` 两档 |
| `Alt` | 按住时触发 action `16` 相关逻辑 |
| 小键盘 `+/-` | 调整一个浮点参数，步进约 `0.015`，具体效果待确认 |

### `sub_4756D0`

| 快捷键 | 初步行为 |
| --- | --- |
| `Ctrl+F` | 打开或切换一个调试输入/面板 |
| `Ctrl+P` | 切换某个运行模式，调用 `sub_489800` |
| `Ctrl+Y` | 配合 `Shift`/`Alt` 修改测试客户端或对象编号，并走 `#CreateClient` 路径 |
| `Ctrl+U` / `Ctrl+T` | 生成或操作测试对象，涉及 `Test_AI` / `Test` 字符串 |
| `Ctrl+1` | 选择调试状态值 `1` |
| `Ctrl+2` | 选择调试状态值 `0` |
| `Ctrl+3` | 选择调试状态值 `6` |
| `Ctrl+4` | 选择调试状态值 `7` |
| `Ctrl+5` | 选择调试状态值 `9` |
| `Ctrl+G` | 将已选择的调试状态应用到当前对象 |
| `Ctrl+H` | 切换一个调试显示或控制模式，调用 `sub_479170` |
| `Ctrl+L` | 切换另一个调试模式，调用 `sub_4899B0` |
| `Ctrl+C` | 对当前对象调用 `sub_424BA0`，具体效果待确认 |
| `Ctrl+D` | 切换调试显示或模式，目标分支仍需命名 |
| `Ctrl+S` | 将当前对象某个数值增加 `50`，调用 `sub_450DA0` |
| `Ctrl+A` | 将同一数值清零 |
| `Ctrl+V` | 按当前位置或参数调用 `sub_4511A0` |

### `sub_4C42B0`

该函数中也存在调试开关相关 UI 热键逻辑。当前只确认它会根据按键触发 `sub_4C3CA0(index)` 一类面板/按钮选择逻辑。

涉及按键：

```text
B
Tab
V
N
Enter
C
数字 1-6
小键盘数字
F1-F6
```

这些按键的具体面板名称或功能需要结合 UI 状态和动态验证确认。

## 待继续确认

- 逐个命令确认参数数量、参数类型和最终效果。
- 从解包脚本、对话、场景触发文本中搜索命令用例，反推参数格式。
- 给 `sub_458410` 的每个命令分支建立地址到命令名的映射表。
- 确认键盘状态数组的精确定义，例如当前按下、刚按下、释放边沿。
- 动态验证调试快捷键的玩家可见效果。
- 调研 `2.01` 是否存在同等调试入口、命令解释器和快捷键逻辑。
