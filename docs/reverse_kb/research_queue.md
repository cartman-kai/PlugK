# 待确认问题

本文件记录尚未完全确认的逆向问题。确认后应迁移到对应专题文件，并在这里删除或标记完成。

## 状态栏左侧裁剪

- 1.05 静态基础见 `systems/status_bar_center.md`。验证严格保留 600 像素时经验条、图标文字和背景装饰的实际边缘；右技能槽模板范围 `[606,649)` 已确认在裁剪外。
- 确认状态栏所有像素提交路径，尤其文字与悬停提示是否需要独立处理；对已有裁剪取交集，不能覆盖怒气等控件的内部裁剪。
- 核对 `sub_4B4800` 特殊区域及普通命中矩形的处理，确认隐藏区不会残留 UI 捕获、提示或阻止移动/攻击，且其它弹窗仍可操作。
- 验证裁除后该区域的场景绘制是否完整；若世界绘制另有底部视口限制，视觉裁剪本身不会扩大世界视口。
- 2.01 底层裁剪和命中函数已完成静态对照并实现，见状态栏专题；1.05 用户反馈生效。继续由用户验证 2.01 的 800×600、原生 1024×768、自定义分辨率、模板重载、快捷键、12 个快捷槽及关闭开关行为。
- 用户反馈 rock-v1 功能正常但锯齿不自然，现改为 rock-v2：上下内收、中央外隆的 3 像素单峰弧线，657～660 像素边界，精灵/GDI/命中同步；随后在裁剪内增加 6 像素 RGB565 背景渐隐，裁剪外仍不写入。待用户验证两版渐隐外观、颜色量化和装备按钮可见性。

## 版本与函数对应

- 确认 2.01 连招得分计算函数，对应 1.05 `sub_423580`。
- 确认 2.01 掉落入口包装函数的函数边界；当前已确认二次掉落调用点 `0x0042CD6B` 会进入 `sub_45E5A0(..., score)`。
- 对照 1.05 `sub_452390` 在 2.01 中的对应函数。
- 调研 2.01 是否存在与 1.05 `sub_4C0670` / `sub_458410` 对应的调试入口和命令解释器。

## D3D7 CPU facade 的 2.01 适配

- 确认 2.01 的 D3D 初始化入口、返回值、调用约定和 `3D Func Error` 失败边界，不能直接复用 1.05 `0x00441FC0`。
- 确认 2.01 的 D3D device 全局、render target 全局以及目标 surface 是否仍使用标准 `IDirectDrawSurface7` COM vtable。
- 使用 D3D7 wrapper 调用日志对照 2.01 的实际 primitive/FVF/state 集合，再决定是否扩展共享 CPU rasterizer。

## 1.05 敌人 AI 与角色动作模板

- 继续映射 `Skill.ini` 尚未命名的自然列和运行时记录偏移；自然列 4、12、22、23、38 及可变子记录布局已经确认。
- 动态记录 `RolePrivate.txt.性格2 -> 对象+0x32D` 在四个难度下的结果，并标定 `sub_42C3C0`、`sub_42E5C0` 使用的各个全局时间基数，得到实际索敌/思考间隔；桶和机关还会在 `sub_42E960` 中直接使用原始值作为特殊动作周期倍率。
- 动态标定 `RolePrivate.txt.索敌范围` 的实际画面距离；静态链已确认 `sub_42D330 -> sub_4204D0` 会把模板值左移 6 位后用于空间查询。
- 确认 `Role.txt.性格1 == 21` 受击后 `sub_423660`、`sub_41AC40`、`sub_41B8B0` 的玩家可见效果，以及复合值 `21*0` 的第二元素在 `sub_420520` 目标过滤中的精确语义；解析器已确认普通整数值为 21、第二复合元素为 0。
- 继续追踪 `sub_42C3C0` 下发的内部动作 opcode 接收端，建立 opcode、技能组和玩家可见动作之间的对应关系。
- 动态验证 `sub_447E30` 的编队半径变化和协同技能组命令在画面中的表现；静态上已确认它不是普通单体攻击选择函数。
- 对照 `BeastieMdl.txt` 的“程序标识/程序数据/高度AI”读取者，明确环境生物体系与战斗敌人 AI 的边界。
- 如静态分析仍不足，先设计低频、只读的动作选择日志方案；未经确认不得修改 plugK Hook。

## 1.05 client/server 遗留架构

- 动态记录正常模式下 `sub_487E50` 生产文本命令、server 侧形成包、`sub_4702D0` 消费 type 1 create-client 包的线程 ID 与先后顺序。
- Hook `CDataPathProperty` 虚表 `off_529710+0x0C` 对应的 `sub_471D90(packet, length)` 做只读包追踪，确认频率、最大长度、缓冲区生命周期和主线程边界。
- 用复制后的包做单进程延迟回放，再做两个本机进程的只观看 client，验证模式 `0` 是否能稳定完成场景和 UI 初始化。
- 逆向客户端逻辑动作如何绑定 client ID 并进入 server 命令路径；这是远端玩家能够独立控制角色的关键门槛。
- 查找开发版、独立 server/client 可执行文件或缺失 DLL，确认 `sub_408AB0` 的 `0/2/3` 模式是否曾由不同构建目标使用。
- 在隔离存档环境中评估强制模式 `0/2/3` 的最小启动行为；当前不应把它当成联机开关用于正式补丁。

## 2.01 client/server 内部包管线

- 动态记录 `0x00497370 -> 0x00494C40`、`0x00493B40 -> 0x00480350 -> 0x0047E720` 的线程 ID、调用顺序和每帧频率。
- 确认 `0x00493B40` 使用的服务端虚表第一个函数是否经过额外本地队列，以及包缓冲区在 `0x00480350` 返回后的生命周期。
- 记录类型 `1/2/3/4/5/7/8/9` 的完整字段类型、包长度、对象索引复用和创建/删除配对关系。
- 在隔离副本中验证强制模式 `0/2/3` 的最小启动行为；当前不应把 `0x0047CAD0` 改为 `3` 作为正式 Mod 方案。
- 区分二进制状态包、文本命令队列和存档序列化，确认是否存在绕过 `0x0047E720` 的直接 Client 更新路径。

## 调试模式

- 逐个确认 1.05 `sub_458410` 中调试命令的参数数量、参数类型和最终效果。
- 从解包脚本、对话、场景触发文本中搜索调试/脚本命令用例，反推参数格式。
- 补充验证 1.05 `Shift+F2` 在 DPI 虚拟化和不同图形包装器下的分辨率切换结果；原生运行环境的玩家可见效果已经确认。
- 确认键盘状态对象中 `byte_548BC8` 起始的第三块 256 字节区域用途。`byte_5489C8` 当前帧状态和 `byte_548AC8` 上一帧按下标记已经由 `sub_4CE3D0` 静态确认，不再列为待确认项。

## 连招二次掉落

- 翻译二次掉落完整逻辑。
- 确认连招得分超过 20 分后的奖励分支。
- 确认 `30 / 30 / 40` 三个得分分量分别代表什么。
- 确认处于连招中击杀敌人的判定变量。

## 自定义难度

- 对 `0x0054C7A8` 设置硬件写入断点，确认新建角色、读档、切图、返回主菜单和通关继承时的生命周期。
- 动态标定 `sub_419000` 中对象字段 `+0x32D` 每一档对应的实际 AI 思考间隔；静态上已确认它由 `RolePrivate.txt.性格2` 按难度换算，并参与索敌和战斗内随机行为。
- 动态确认 `sub_41D650` 中按 `40/20/8/2` 缩放并写入 `this+0x3A2` 的资源类型和触发场景。
- 动态确认玩家、友方、召唤物是否也经过 `sub_4247E0` 的属性倍率入口；该函数读取的 `RolePrivate.txt` 自然列号 10/11/12/14 已确认为 `HP/AT/DF/命中率`。
- 确认 `sub_423FB0` 中难度 2/3 的 `-6/-12` 修正作用于哪一项连招击杀奖励。
- 动态确认 `sub_447E30` 两处难度概率分支对应的 AI/群体行为，并验证特殊难度值 100 是否只用于调试。
- 确认 `sub_458410` 的 `AddItem` 分支参数格式，以及难度 0 使用 `0.7` 系数时具体缩放的两个数值参数。
- 动态验证 `sub_47D850` 的本级经验进度损失、`sub_47F560` 的死亡金钱回收比例。
- 动态验证 `sub_47F740` 的两类失败：结果权重未命中时是否按 `PropUpgrade.txt.失敗損失` 消耗原料并显示文本 ID 311；选中结果超过难度门槛 `7/8/9/15` 时是否保留全部原料并显示文本 ID 310。
- 复核 `inheritance.c` 把 1.05 `0x0049E39F` 比较立即数从 3 改为 5 后的实际通关继承行为。
- 在 2.01 程序中取得 `0x0057D360` 的全部交叉引用，并与 1.05 的 14 个读取点逐项对应。

## Type 35

- 确认 Type 35 是否只在 2.01 或特定资源表中存在。
- 确认是否有调用方显式传入 Type 35 查询物品表。
- 确认高难度、特殊脚本或任务逻辑是否会生成 Type 35。

## 地面物品自动拾取

- 1.05/2.01 自动拾取异常路径仍需手工回归：自动拾取开启时切图、读档/返回主菜单、背包满、敌人刚死亡后掉落。
- 2.01 `type=35` 宝石与 `type=50` 护身石已纳入自动拾取模式设计，但仍建议补充动态样本，确认其地面对象字段与 `PropMdl` 类型读取路径一致。

## 2.01 地面物品名称颜色优化

- 动态验证 2.01 中 `0x0041A5D3` 改为 `push 1` 后地面物品名称无背景，且不影响其他浮动文字。
- 动态验证 2.01 颜色优化对 `type=30/35/50` 的地面物品名生效，并确认背包丢出护身石后的显示。
- 动态验证 2.01 背包悬停物品、NPC 名字等共享文字路径不会被内部标记方案串色。
- 继续确认 `type=35` 的自然生成路径。

## 角色对象

- 确认 `Init.txt` 的潜力是否原本对应运行时技能点 `+0x34C`、声望的运行时落点，以及模板怒气值非 0 时初始化逻辑是否仍固定把怒气上限清零。
- 确认 `DF3..DF0` 是否用于方向、动作状态或非玩家战斗对象的命中/防御虚函数。
- 如需量化 DF4 对命中率的影响，在 `sub_4236B0` 命中判定点记录 `sub_423260(victim) * DF4 / 100` 的随机上限、随机余数和攻击者命中阈值；当前已静态确认 DF4 的用途和影响方向，但有限样本不能给出概率。
- 动态抽样确认 2.01 角色数据对象初始化后的基础属性值与 `Init.txt` 记录一致；对象大小、偏移和初始化函数已完成静态对照。
- 整理难度、地图、任务状态与角色对象或全局变量的关系。
- 确认物品池数组容量、生命周期和释放规则。

## 连续受击保护

- `+0x1A7` 的战斗阵营/分组语义和 AI 选敌作用已经动态确认：玩家从 `10001` 改为敌人分组 `3` 后，敌人完全停止攻击。继续寻找正常玩法中能够让同组伤害实际进入结算的路径，以判断减半分支是可触发的友伤保护还是通用兜底。
- `RoleEffectMdl` ID 44“力士无敌”已经动态确认把伤害从 `50` 独立减为 `25`。强制标志不模拟原版技能赋予和持续时间，原版技能如何设置、维持及清除该效果仍待确认。
- 状态 110“受伤旋飞”的自动清空已确认，稳定 `return_rva=0x0001B618`；继续确认普通倒地、硬直恢复、玩家死亡等其它状态是否还有不同的整体清空路径。
- 在捕获持续开启时跨场景切换，定位玩家观察到的切图清零具体由哪个 `sub_423490` 调用来源完成；现有切图后日志开始时已经重新登记了四个来源。
- 查找 `+0x432` 独立归零的写入点和触发条件；动态样本确认它可在 `+0x422` 仍为 1 时从 1.0 回到 0.0。
- 用 2.01 的最短受击样本动态确认 `sub_42BF60`、`sub_42BB20`、`sub_42BD40` 及 `+0x42E/+0x43E` 字段；静态对应和公式已经确认。
- 动态确认 2.01 效果 113“罡气护体”的自然赋予/清除入口，并验证单独启用时主伤害是否稳定钳为 1；静态代码还显示效果 44 与 113 同时启用时只执行效果 44。
- 为常驻减伤 HUD 确认一个 1.05 每帧恰好一次、目标为游戏 HUD surface 的文字绘制调用边界；若复用 `sub_4B25A0`，必须接入 `item.c` 现有共享 Hook，不能重复安装。

## 技能与招式

- 确认更上层、每帧稳定存在的战斗 controller update 函数，用于替代右键路径缓存 controller。
- 对 2.01 必杀技释放入口做动态验证，确认 `target == NULL` 时的兜底行为和 1.05 完全一致。
- 抽样确认不同角色、不同必杀技是否都可以只传 `target == NULL`，由释放入口内部补上下文。

## 数据表

- 为 `PropMdl.txt` 整理字段表。
- 整理掉落配置表来源与字段含义。
- 编写可重复的数据表统计方法，避免手工统计结果漂移。
