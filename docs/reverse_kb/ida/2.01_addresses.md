# IDA 2.01 地址表

本文件记录 2.01 版本中已确认或待确认的地址，用于和 1.05 对照移植。

## 全局变量

| 地址 | 说明 | 来源 |
| --- | --- | --- |
| `0x00589A44` | 保存角色对象指针 | 动态调试 |
| `0x005788D0` | `PropMdl` 表地址，表头形态为 `[count, base]` | 动态调试 |
| `0x00578D18` | `RandProp` 表地址，表头形态为 `[count, base]` | IDA 静态分析 |
| `0x0057D360` | 难度变量 | 已整理 |
| `0x0058D164` | 技能/快捷栏管理对象指针 | 静态分析 |
| `0x00589E34` | 当前地面/地图管理器指针，地面创建和自动拾取 Z this 校验使用 | 动态调试 |
| `0x00589E38` | 当前场景 6 字节对象槽表指针；槽位 `+2` 为对象指针 | 动态调试 |

## 字体与文字渲染

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4C7640` / `0x004C7640` | 读取 `Font.txt`，基于系统菜单 `LOGFONTA` 创建全局字体数组；字体替换 Hook 目标 | IDA 静态分析 |
| `0x004C77C0` | `CreateFontIndirectA` 调用点；调用前 `[ESP]` 为 `LOGFONTA *`；原版实测高度 `-12/-12/-24/-12`、宽度 `0`、字重 `400`、charset `1`、quality `0` | IDA 静态分析、x32dbg 动态调试 |
| `0x0058C44C` | 6 槽 `HFONT` 数组；当前资源实际填充前 4 槽 | IDA 静态分析、x32dbg 动态调试 |
| `sub_4C6A80` / `0x004C6A80` | 清理字体数组，对非空句柄调用 `DeleteObject` | IDA 静态分析 |
| `sub_4C5710` / `0x004C5710` | 选择字体并通过 `GetTextExtentPoint32A` 测量 | IDA 静态分析 |
| `sub_4C5790` / `0x004C5790` | 选择字体并通过 `DrawTextA` 输出矩形/多行文字 | IDA 静态分析 |
| `sub_4C58F0` / `0x004C58F0` | 通用 `TextOutA` 文字输出；字体索引位于文字对象 `+0x78` | IDA 静态分析 |
| `0x004C57DA` / `0x004C58B8` | 矩形文字路径通过 DirectDraw surface 虚表 `+0x44/+0x68` 调用 `GetDC/ReleaseDC` | IDA 静态分析 |
| `0x004C57FC` | 矩形文字路径调用 `SetBkMode(hdc, TRANSPARENT)` | IDA 静态分析 |
| `0x004C5897` | `DrawTextA` 调用点 | IDA 静态分析 |
| `0x004C593E` / `0x004C5C19` | 通用文字路径通过 DirectDraw surface 虚表 `+0x44/+0x68` 调用 `GetDC/ReleaseDC` | IDA 静态分析 |
| `0x004C5964` | 通用文字路径调用 `SetBkMode(hdc, TRANSPARENT)` | IDA 静态分析 |
| `0x004C597A` | 通用文字路径的 `SelectObject` 调用点 | IDA 静态分析 |
| `0x004C5BB6` | 通用文字路径的 `TextOutA` 调用点 | IDA 静态分析 |

## 技能与招式相关函数

| 函数/地址 | 说明 | 1.05 对应 |
| --- | --- | --- |
| `sub_483B40` / `0x00483B40` | 技能/招式释放入口，`thiscall`，`ECX` 为战斗/输入 controller | `0x004752C0` |
| `sub_482C50` / `0x00482C50` | 右键释放路径，取右手/准备招式后调用释放入口 | `0x004743D0` |
| `sub_4D86C0` / `0x004D86C0` | 获取当前右手或准备释放的招式 ID | `0x004C40E0` |
| `sub_4D8790` / `0x004D8790` | 手势识别成功后写入准备释放招式 ID | `0x004C41B0` |
| `sub_4D87D0` / `0x004D87D0` | 设置当前右手招式 ID | `0x004C41F0` |
| `sub_4D8810` / `0x004D8810` | 刷新右手/准备招式显示资源 | `0x004C4230` |
| `sub_424F80` / `0x00424F80` | 从 controller 关联对象取得 target/context | `0x0041C9A0` |

## 开场动画与视频播放

### 非 Steam `ComeOn.exe`

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_40B340` / `0x0040B340` | 主循环状态机；state 6 从 `this+0x284` CString 取路径并播放开场动画 | IDA 静态分析、动态调试 |
| `0x0040B473` | state 6 调用 `sub_522730(path, 0)` 的调用点；动态确认路径为 `...\dhp\begin.dhp` | 动态调试 |
| `sub_522730` / `0x00522730` | 按路径启动视频播放的 `thiscall` 包装；plugK 跳过开场动画 Hook 目标 | IDA 静态分析、动态调试 |
| `sub_523890` / `0x00523890` | DirectShow/COM 初始化，调用 `CoInitialize`、`CoCreateInstance`、`QueryInterface` | IDA 静态分析 |
| `sub_523AD0` / `0x00523AD0` | `MultiByteToWideChar` 后通过 DirectShow COM vtable 渲染/播放路径 | IDA 静态分析 |

### Steam `ComeOn.dll`

IDA 默认基址为 `0x10000000`。动态调试样本中 `launcher.exe` 进程内 `ComeOn.dll` 基址为 `0x6E6C0000`。

| 函数/地址 | RVA | 说明 | 来源 |
| --- | --- | --- | --- |
| `dll_DirectShow_play_media` / `0x10003E00` | `0x3E00` | `ComeOn.dll` 唯一导出；创建线程调用 `sub_10003A80(param)` | IDA 静态分析 |
| `sub_10003A80` / `0x10003A80` | `0x3A80` | Steam launcher 阶段独立 DirectShow 播放线程；解析 `"RenderFile"` 与 `"put_Caption"`，窗口标题样本为 `ComeOn-begin-dhp` | IDA 静态分析、动态调试 |
| `0x10003E0B` | `0x3E0B` | `dll_DirectShow_play_media` 中压入 `sub_10003A80` 作为 `CreateThread lpStartAddress` | IDA 静态分析 |
| `0x10003E14` | `0x3E14` | `dll_DirectShow_play_media` 调用 `CreateThread` | IDA 静态分析 |
| `0x10003AFC` | `0x3AFC` | 搜索宽字符串 `"RenderFile":"` | IDA 静态分析 |
| `0x10003BBF` | `0x3BBF` | 搜索宽字符串 `"put_Caption":"` | IDA 静态分析 |
| `0x10003C29` | `0x3C29` | 调用 `CoCreateInstance` 创建 DirectShow filter graph | IDA 静态分析 |
| `0x10003C7F` | `0x3C7F` | 调用 COM vtable `+0x34`，对应 `IGraphBuilder::RenderFile` | IDA 静态分析 |
| `0x10003D23` | `0x3D23` | `FindWindowExW(..., put_Caption)` 查找播放窗口 | IDA 静态分析 |

## 输入与快捷键相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_40CEC0` / `0x0040CEC0` | 每帧输入处理入口；刷新键盘状态后调用鼠标、战斗输入和快捷键消费逻辑 | IDA 静态分析 |
| `sub_4E3280` / `0x004E3280` | 键盘状态刷新；维护上一帧标记并调用 `GetKeyboardState(this + 8)` | IDA 静态分析 |
| `byte_578F78` / `0x00578F78` | 键盘状态对象；`+8+VK` 为当前 VK 状态，`+0x108+VK` 为上一帧/已按下标记 | IDA 静态分析 |
| `sub_4D8890` / `0x004D8890` | 快捷栏键位分发；`1..6`/小键盘 `1..6` 映射到 `sub_4D8260(0..5)` | IDA 静态分析 |
| `sub_4D8260` / `0x004D8260` | 快捷栏槽位执行；`slot < 6` 查询槽位物品并调用 `sub_48D890` 执行动作；plugK 的 Alt+1..4 屏蔽 hook 目标 | IDA 静态分析 |
| `0x004D8BBA` | `sub_4D8890` 调用 `sub_4D8260(slot)` 后的返回地址；plugK 用于限定只屏蔽键盘数字键路径 | IDA 静态分析 |
| `0x004D838C` | `sub_4D8360` 调用 `sub_4D8260(slot)` 后的返回地址；该路径来自 UI/鼠标槽位点击，不应被 Alt+1..4 键盘屏蔽误伤 | IDA 静态分析 |

## 背包界面相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4DB730` / `0x004DB730` | 背包初始化虚函数，注册模板子控件 `21/22/23/90` | IDA 静态分析 |
| 背包虚表 `0x00553C20` | 2.01 背包界面对象虚表 | x32dbg 动态调试 |
| `sub_4DBAD0` / `0x004DBAD0` | 背包绘制虚函数，plugK 2.01 当前页整理按钮 Hook 目标；关闭时返回 `0`，实际绘制时返回 `1` | IDA 静态分析、x32dbg 动态调试 |
| `0x004DBB0F` | 背包可见性检查通过后的实际绘制路径起点 | x32dbg 动态调试 |
| 渲染上下文 vfunc `+0x34`（动态样本 `sub_517990` / `0x00517990`） | 按表面位深填充矩形；16 位模式直接写 WORD 像素 | IDA 静态分析、x32dbg 动态调试 |

## 技能界面相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4BD300` / `0x004BD300` | 技能界面类构造函数；写入虚表 `0x00553438`，并把对象保存至 `0x0058BDE8` | IDA 静态分析 |
| `sub_4BECA0` / `0x004BECA0` | 技能界面初始化虚函数，注册模板子控件 `125..147` 与 `227..246` | IDA 静态分析 |
| `sub_4BE0B0` / `0x004BE0B0` | 技能界面绘制虚函数；plugK 2.01 技能重置按钮 Hook 目标 | IDA 静态分析、x32dbg 动态调试 |
| `0x004BE0E0` | 技能界面可见性检查通过后的实际绘制路径起点 | x32dbg 动态调试 |
| `sub_4BD630` / `0x004BD630` | 技能界面点击处理；`126` 为关闭、`127/128` 为页签 | IDA 静态分析 |
| `sub_4BE9A0` / `0x004BE9A0` | 技能界面及子控件重新定位；确认原点字段为对象 `+0x14/+0x18` | IDA 静态分析 |
| `dword_58BDE8` / `0x0058BDE8` | 当前技能界面对象指针 | IDA 静态分析、x32dbg 动态调试 |
| 技能界面虚表 `0x00553438` | `+0x08` 指向绘制函数 `sub_4BE0B0` | IDA 静态分析、x32dbg 动态调试 |

## 掉落相关函数

| 函数/地址 | 说明 | 1.05 对应 |
| --- | --- | --- |
| `sub_45E5A0` | 随机掉落生成函数，内部按掉落配置计算金钱/物品，并调用具体选物函数 | `sub_452420` |
| `sub_45E390` | 根据等级/类型/难度从物品表选具体物品 ID | `sub_452200` |
| `sub_49AB70` | 创建并投放地面物品 | `sub_48B4F0` |
| `sub_5264DF` / `0x005264DF` | 游戏内部 `rand` 实现 | `0x0050E17A` |

## 地面物品名称显示

以下地址来自 2.01 IDA 静态对照，玩家可见效果仍需动态验证。

| 2.01 函数/地址 | 说明 | 1.05 对应 |
| --- | --- | --- |
| `sub_41A500` / `0x0041A500` | 地面物品名称显示函数；读取 `this+0x81` 的 `PropMdl` 记录，经 `sub_4E4D90(record, 0)` 取名称 | `sub_4127D0` / `0x004127D0` |
| `0x0041A5D3` | 调用 `sub_4E1270` 前压入节点 `+0x0C` 样式参数；改为 `push 1` 预计可去除背景 | `0x004128A3` |
| `sub_4E1270` / `0x004E1270` | 创建浮动文字节点，节点大小 `0x1C`，写入 `+0x0C/+0x10/+0x14` | `sub_4CC4B0` / `0x004CC4B0` |
| `sub_4E1200` / `0x004E1200` | 地面浮动文字管理器绘制入口；遍历节点，`node+0x0C == 1` 时关闭背景 | `sub_4CC440` / `0x004CC440` |
| `sub_4C5140` / `0x004C5140` | 通用文字绘制包装函数，最终调用 `sub_4C58F0` | `sub_4B1DF0` / `0x004B1DF0` |
| `sub_4C58F0` / `0x004C58F0` | 最终文字输出函数；先 `SetTextColor(hdc, this[24])`，默认颜色仍为对象 `+0x60`；支持 `$G/$Y/$B/$R/$W/$H` 内嵌颜色码 | `sub_4B25A0` / `0x004B25A0` |
| `0x005751D8..0x005751F0` | `$G/$Y/$R/$B/$W/$H` 使用的 `COLORREF` 常量；精确映射见文字渲染专题 | `0x00545C48` 附近 |

移植策略：复用 1.05 的单条字符串内部标记方案，只对地面物品名临时改色并立即恢复共享文字颜色；2.01 目标类型为 `type=30/35/50`。

## 地面物品拾取相关函数

以下地址来自 2.01 动态调试与 IDA 静态对照。拾取链路详情见 [物品系统](../systems/items.md)。

| 2.01 函数/地址 | 说明 | 1.05 对应 |
| --- | --- | --- |
| `sub_4247F0` / `0x004247F0` | 行为入口，`thiscall`，`a2 == 22` 为原版 Z 拾取周围地面物品 | 待确认 |
| `0x00424E64` | Z 分支调用 `sub_45E8B0` 后的返回地址，可作为自动拾取过滤边界 | 待确认 |
| `sub_45E8B0` / `0x0045E8B0` | 地面物品接触/触发拾取处理函数；会解析角色对象、调用地面物品虚表 `+0x20`，再调用 `sub_48DCE0` | 待确认 |
| `sub_48DCE0` / `0x0048DCE0` | 地面物品入包包装函数；从地面对象字段构造 `sub_48DD30` 参数 | 待确认 |
| `sub_41A610` / `0x0041A610` | 2.01 地面物品虚表 `off_5519FC + 0x20`；拾取前注销或标记地面对象状态 | 待确认 |
| `sub_41A0D0` / `0x0041A0D0` | 地面物品析构/释放路径；普通物品拾取后观察到以参数 `1` 调用 | 待确认 |
| `sub_4E4230` / `0x004E4230` | 对象管理器/队列清理路径；满背包拾取样本中稍后通过 `vfunc[0]` 调用 `sub_41A0D0(..., 1)` 释放原地面对象 | 待确认 |
| `off_5519FC` / `0x005519FC` | 2.01 地面物品虚表；对象字段 `+0x67 == 23` | 待确认 |
| `off_551E2C` / `0x00551E2C` | 2.01 Z 行为对象虚表，自动拾取通过 `this+4 == *0x00589E34` 二次校验 | 动态调试 |

## 随机掉落调用边界

`sub_45E5A0` 的第一个参数会按 `RandProp.角色ID` 查询 `0x00578D18` 表。已确认调用点：

| 地址 | 行为 | 说明 |
| --- | --- | --- |
| `0x0043F6B1` -> return `0x0043F6B6` | 调用 `sub_45E5A0(..., 0.0)` | 普通击败/血量清空时的一次掉落 |
| `0x0042CD6B` | 调用 `sub_45E5A0(..., score)` | 连招结束后的二次掉落路径 |
| `0x0045E6F1` -> return `0x0045E6F6` | `rand` 后进行物品掉落 gate 判定 | 可用于限定敌人基础物品掉落概率判定 |

## 掉落选物关键地址

`sub_45E390(a1, a2)` 中，只有 `a2 == -1` 或 `a2 == 0xFFFF` 时才会随机决定物品类型。

| 地址 | 指令/行为 | 说明 |
| --- | --- | --- |
| `0x0045E417` | `mov esi, 1Eh` | 生成 Type 30 |
| `0x0045E447` | 设置 Type 10 或 Type 20 | 未命中 Type 30 后的分支 |

## `sub_45E5A0` 内部调用 `sub_45E390` 的返回地址

这些返回地址用于把 hook 限定在 2.01 敌人随机掉落路径内：

- `0x0045E7B0`
- `0x0045E7C1`
- `0x0045E7D2`
- `0x0045E7E3`

## 待确认

- 连招得分计算函数，对应 1.05 `sub_423580`。
- `sub_45E5A0` 内部各分支与 1.05 `sub_452420` 的逐段对应关系。
