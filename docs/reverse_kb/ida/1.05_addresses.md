# IDA 1.05 地址表

当前 IDA Pro 中主要链接的是 1.05 版本程序。本文件只记录 1.05 特定地址。

## 全局变量

| 地址 | 说明 | 来源 |
| --- | --- | --- |
| `0x00558AC4` | 保存角色对象指针 | 已整理 |
| `0x00548644` | 调试模式全局开关，开启后值为 `1` | IDA 静态分析 |
| `0x00558E4C` | 当前地面/地图管理器指针，地面创建和自动拾取 Z this 校验使用 | 动态调试 |
| `0x00558E50` | 当前场景 6 字节对象槽表指针；槽位 `+2` 为对象指针 | 动态调试 |
| `0x0055BBB0` | 技能/快捷栏管理对象指针 | 动态调试 |
| `dword_548790` / `0x00548790` | `player/Init.txt` 运行时表头 | IDA 静态分析 |
| `dword_54C7A8` / `0x0054C7A8` | 当前难度，原版有效值 `0..3`；只有 `sub_4A2EA0` 写入，全部静态读取点见难度系统专题 | IDA 静态分析 |

## 角色初始化与受击结算

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_405300` / `0x00405300` | 启动数据表加载；拼接并加载 `player\init.txt` 到 `0x00548790` | IDA 静态分析 |
| `sub_455160` / `0x00455160` | 存档读取辅助函数；分配 `0x378` 字节临时角色数据对象并反序列化存档 | IDA 静态分析 |
| `sub_47D3E0` / `0x0047D3E0` | 角色数据对象构造函数 | IDA 静态分析 |
| `sub_4890A0` / `0x004890A0` | 无现成对象时分配并构造全局角色数据对象，写入 `dword_558AC4` | IDA 静态分析 |
| `sub_47E8B0` / `0x0047E8B0` | 新角色初始化包装；清空槽位后依次调用模板绑定和属性初始化 | IDA 静态分析 |
| `sub_47E8F0` / `0x0047E8F0` | 按角色 ID 查询 Init 记录，写入 `this+0x308`；初始化属性倍率 | IDA 静态分析 |
| `sub_47E5A0` / `0x0047E5A0` | 从 Init 记录复制新角色基础属性、金钱、初始物品和技能 | IDA 静态分析 |
| `sub_47E040` / `0x0047E040` | 按 Init 的升级增加值累加角色基础属性 | IDA 静态分析 |
| `sub_480AC0` / `0x00480AC0` | 按索引写入 `this+0x2C8..0x2DC` 的六个属性倍率 | IDA 静态分析 |
| `sub_480D70` / `0x00480D70` | 增加角色数据对象 `+0x50` 的怒气上限，封顶 9999 | IDA 静态分析 |
| `sub_42FF40` / `0x0042FF40` | 玩家战斗对象虚函数；返回关联角色数据对象 `+0x50` 的怒气上限 | IDA 静态分析 |
| `sub_424EC0` / `0x00424EC0` | 增减战斗对象 `+0x3B6` 的当前怒气，并夹紧到怒气上限 | IDA 静态分析 |
| `sub_424770` / `0x00424770` | 增减战斗对象 `+0x3A2` 的当前 HP，并夹紧到最大 HP | IDA 静态分析 |
| `sub_424E90` / `0x00424E90` | 用战斗对象 `+0x3A6` 和最大 MP 计算当前 MP 百分比 | IDA 静态分析 |
| `sub_424DE0` / `0x00424DE0` | 用战斗对象 `+0x3AA` 和最大 Stamina 计算当前 Stamina 百分比 | IDA 静态分析 |
| `sub_42FEE0` / `0x0042FEE0` | 玩家 Stamina 恢复量虚函数；按需读取 Init 字段 19 | IDA 静态分析 |
| `sub_42FF20` / `0x0042FF20` | 玩家 Stamina 消耗率虚函数；按需读取 Init 字段 20 | IDA 静态分析 |
| `sub_42FE20` / `0x0042FE20` | 玩家战斗对象 vtable `+0x90` 实现；固定读取关联玩家 Init 字段 6（DF4） | IDA 静态分析 |
| `sub_419E80` / `0x00419E80` | 上层受击协调；调用伤害结算后登记招式和攻击来源 | IDA 静态分析 |
| `sub_4236B0` / `0x004236B0` | 命中和伤害结算；用战斗对象 `+0x422` 的来源数除伤害 | IDA 静态分析、1.05 有限捕获动态验证 |
| `sub_420D50` / `0x00420D50` | 按运行时角色句柄从场景表取得战斗对象 | IDA 静态分析 |
| `sub_42FB30` / `0x0042FB30` | 玩家战斗对象构造；设置 vtable `off_528CD4`，并把玩家数据索引设为 1 | IDA 静态分析 |
| `sub_42FDB0` / `0x0042FDB0` | 从玩家战斗对象 `+0x18` 的索引反查玩家数据对象 | IDA 静态分析 |
| `sub_4232E0` / `0x004232E0` | 维护招式 ID 与攻击来源键集合，更新本轮受击计数 | IDA 静态分析、1.05 有限捕获动态验证 |
| `sub_423490` / `0x00423490` | 清空本轮招式、攻击来源集合和受击计数；手动和状态 110 自动路径均已验证 | IDA 静态分析、1.05 有限捕获动态验证 |
| `sub_421F40` / `0x00421F40` | 战斗对象分组 setter；写入 `this+0x1A7` 前调用 `sub_421FA0` 清理关联状态，内置 `SetGroup`、初始化和继承路径均调用 | IDA 静态分析 |
| `sub_421FA0` / `0x00421FA0` | 分组等对象状态变化时清零 `this+0x143`，并按需清零 `this+0x3D2` 所指对象的 `+0x28` | IDA 静态分析 |
| `sub_41B380` / `0x0041B380` | 玩家受击状态处理；参数 4 分支写入状态 110“受伤旋飞”并调用 `sub_41B600(this, 0)` | IDA 静态分析、1.05 有限捕获动态验证 |
| `sub_41B600` / `0x0041B600` | 玩家状态 110 等动作的处理函数；参数 0 入口先调用 `sub_423490(this, 0)` 清空受击保护 | IDA 静态分析、1.05 有限捕获动态验证 |
| `sub_42B310` / `0x0042B310` | 读取战斗对象 `this+0x1AF+effect_id` 的效果标记 | IDA 静态分析 |
| `sub_466ED0` / `0x00466ED0` | 从 `0x00558E50` 对象槽表解析攻击来源对象 | IDA 静态分析 |
| `sub_466EB0` / `0x00466EB0` | 过滤攻击来源对象类型；对象 `+0x14` 随后作为来源集合键 | IDA 静态分析 |
| `0x0041A1EA` | `sub_419E80` 调用 `sub_4236B0` 的唯一已知边界 | IDA 静态分析 |
| `0x0041A594` | 本次伤害结算后调用 `sub_4232E0` 的边界 | IDA 静态分析 |
| `0x00423819` | 比较受击者与攻击来源的 `+0x1A7`；相等时伤害先减半 | IDA 静态分析 |
| `0x00423836` | 读取 `victim+0x422` 来源数；适合动态观察隐藏减伤 | IDA 静态分析 |
| `0x00423848` | 保存来源数量除法后的阶段伤害；后续状态效果仍可继续修正 | IDA 静态分析 |
| `0x00423A40` | 检查效果 ID 44（`RoleEffectMdl`“力士无敌”）；启用时伤害再减半 | IDA 静态分析、数据表验证 |
| `0x0041B5AA` | `sub_41B380` 参数 4 分支把玩家战斗状态写为 110（`0x6E`） | IDA 静态分析 |
| `0x0041B613` / 返回点 `0x0041B618` | `sub_41B600(this, 0)` 调用 `sub_423490(this, 0)`；自动重置日志稳定 `return_rva=0x0001B618` | IDA 静态分析、1.05 有限捕获动态验证 |

属性和对象偏移见[角色运行时内存](../runtime/player.md)，完整公式和动态验证方案见[受击、命中与连续受击保护](../systems/combat_damage.md)。

## 难度相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4A2E30` / `0x004A2E30` | 创建角色界面的控件点击分发；控件 159/179 调整难度，160 确认创建 | IDA 静态分析 |
| `sub_4A3050` / `0x004A3050` | 难度循环选择；未解锁时 `0..2`，解锁状态下 `0..3` | IDA 静态分析 |
| `sub_4A31B0` / `0x004A31B0` | 写入难度控件值，并读取文本 ID `difficulty + 115` | IDA 静态分析 |
| `sub_4A2EA0` / `0x004A2EA0` | 创建角色确认；把控件 159 的 `+0xC4` 写入 `dword_54C7A8` | IDA 静态分析 |
| `sub_4A3410` / `0x004A3410` | 创建角色界面初始化，注册难度相关控件 | IDA 静态分析 |
| `0x0049E2B1` | `sub_49E260` 把当前难度复制到流程对象 `+0xC8` | IDA 静态分析 |
| `0x0049E39F` | 通关/继承流程比较对象 `+0xC8` 与 3；当前 plugK 继承补丁会修改此立即数，需动态复核 | IDA 静态分析 |

完整难度读取点、默认分支风险和自定义难度设计见 [难度系统](../systems/difficulty.md)。

## 技能与招式相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4752C0` / `0x004752C0` | 技能/招式释放入口，`thiscall`，`ECX` 为战斗/输入 controller | 动态调试 |
| `sub_4743D0` / `0x004743D0` | 右键释放路径，取右手/准备招式后调用释放入口 | 动态调试 |
| `sub_4C40E0` / `0x004C40E0` | 获取当前右手或准备释放的招式 ID | 静态分析 |
| `sub_4C41B0` / `0x004C41B0` | 手势识别成功后写入准备释放招式 ID | 动态调试 |
| `sub_4C41F0` / `0x004C41F0` | 设置当前右手招式 ID | 静态分析 |
| `sub_4C4230` / `0x004C4230` | 刷新右手/准备招式显示资源 | 静态分析 |
| `sub_41C9A0` / `0x0041C9A0` | 从 controller 关联对象取得 target/context | 动态调试 |
| `sub_41C270` / `0x0041C270` | 行为入口，`thiscall`，`a2 == 22` 为原版 Z 拾取周围地面物品 | IDA 静态分析、动态调试 |
| `0x0041C892` | Z 分支调用 `sub_452730` 后的返回地址，可作为自动拾取过滤边界 | 动态调试 |

## 技能界面相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4AA680` / `0x004AA680` | `JM_SKILL_MAIN` / ID 125 专用技能界面类构造函数 | IDA 静态分析、x32dbg 动态验证 |
| `dword_55AA38` / `0x0055AA38` | 1.05 技能界面对象指针 | IDA 静态分析、x32dbg 动态验证 |
| `sub_4AC050` / `0x004AC050` | 技能界面初始化，注册模板子控件 125..147、207..218 | IDA 静态分析、x32dbg 动态验证 |
| `sub_4AB410` / `0x004AB410` | 技能界面绘制虚函数；plugK 1.05 重置技能按钮 Hook 目标 | IDA 静态分析、x32dbg 动态验证 |
| `0x004AB440` | 父类可见性检查通过后的实际绘制分支入口 | IDA 静态分析、x32dbg 动态验证 |
| `sub_4AA9B0` / `0x004AA9B0` | 技能界面原版控件点击处理；126 关闭，127/128 切换页签 | IDA 静态分析 |

## 输入与快捷键相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_405FB0` / `0x00405FB0` | 主运行输入处理入口；刷新键盘状态后处理鼠标、战斗输入和快捷键 | IDA 静态分析 |
| `sub_4CE3D0` / `0x004CE3D0` | 键盘状态刷新；维护上一帧标记并调用 `GetKeyboardState(this + 8)`，plugK 输入屏蔽 hook 目标 | IDA 静态分析 |
| `byte_5489C0` / `0x005489C0` | 键盘状态对象；`+8+VK` 为当前 VK 状态，`+0x108+VK` 为上一帧/已按下标记 | IDA 静态分析 |
| `sub_4C42B0` / `0x004C42B0` | 快捷栏键位分发；`1..6`/小键盘 `1..6` 映射到 `sub_4C3CA0(0..5)` | IDA 静态分析 |
| `sub_4C3CA0` / `0x004C3CA0` | 快捷栏槽位执行入口；`__thiscall`，`slot` 为栈参数，`slot < 6` 会执行快捷栏物品动作；plugK 的 Alt+1..4 屏蔽 hook 目标 | IDA 静态分析 |
| `0x004C45DA` | `sub_4C42B0` 调用 `sub_4C3CA0(slot)` 后的返回地址；plugK 用于限定只屏蔽键盘数字键路径 | IDA 静态分析 |
| `0x004C3DCC` | `sub_4C3DA0` 调用 `sub_4C3CA0(slot)` 后的返回地址；该路径来自 UI/鼠标槽位点击，不应被 Alt+1..4 键盘屏蔽误伤 | IDA 静态分析 |

## 背包界面相关函数

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4C6DC0` / `0x004C6DC0` | `JM_DAOJU` / ID 20 专用背包界面类构造函数 | IDA 静态分析 |
| `dword_55BDC8` / `0x0055BDC8` | 1.05 背包界面对象指针 | IDA 静态分析、x32dbg 运行时观察 |
| `sub_4C6ED0` / `0x004C6ED0` | 背包界面初始化，注册模板子控件 21/22/23/90 | IDA 静态分析 |
| `sub_4B2ED0` / `0x004B2ED0` | 按模板 ID 创建子控件并加入父界面控件容器 | IDA 静态分析 |
| `sub_4C7260` / `0x004C7260` | 背包绘制虚函数，参数为游戏渲染上下文；plugK 1.05 当前页整理按钮 Hook 目标；界面关闭时仍可能调用但返回 `0` | IDA 静态分析、x32dbg 动态验证 |
| 渲染上下文 vfunc `+0x34`（1.05 当前实现 `sub_500140`） | 按当前表面位深填充矩形；16 位模式直接写 WORD 像素 | IDA 静态分析、x32dbg 动态验证 |

## 开场动画与视频播放

### 非 Steam `comeon.exe`

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_50B9D0` / `0x0050B9D0` | DirectShow/COM 初始化包装，调用 `CoInitialize`、`CoCreateInstance` 并查询播放相关接口 | IDA 静态分析 |
| `sub_50BC10` / `0x0050BC10` | DirectShow 路径播放入口，调用 `MultiByteToWideChar` 后通过 COM 接口渲染并运行文件 | IDA 静态分析 |
| `sub_50A8E0` / `0x0050A8E0` | 按路径启动视频播放的 `thiscall` 包装；状态 6 播放开场视频时调用，plugK 跳过开场动画 Hook 目标；Steam 1.05 `ComeOn.exe` 同样存在该函数 | IDA 静态分析、动态调试 |
| `sub_4043E0` / `0x004043E0` | 主循环状态机；状态 6 从 `this+0x284` 的 CString 取视频路径，调用 `sub_50A8E0` 后清空 CString | IDA 静态分析、动态调试 |
| `sub_404C80` / `0x00404C80` | 把视频路径写入 `this+0x284` 并切到 state 6 的排队函数；`sub_408550` 通过它提交 `dhp\begin.dhp`。曾作为上游 Hook 目标，人工验证显示范围过早，会导致 Steam 1.05 跳过动画后卡住，当前仅作为观察点 | IDA 静态分析、人工验证 |
| `0x00404518` | 状态 6 中 `push 0; push eax; call sub_50A8E0` 的播放调用边界；动态命中时 `eax = E:\GAMES\刀剑封魔录\\dhp\begin.dhp` | 动态调试 |
| `0x00404520` | Steam 1.05 `ComeOn.exe` 中 `sub_4043E0` 调用 `sub_50A8E0` 的调用边界 | IDA 静态分析 |
| `sub_408550` / `0x00408550` | Steam 1.05 `ComeOn.exe` 中拼接 `"%s\dhp\begin.dhp"` 的函数 | IDA 静态分析 |
| `0x004085CA` | Steam 1.05 `ComeOn.exe` 中 `push "%s\dhp\begin.dhp"` 的字符串引用 | IDA 静态分析 |
| `0x004085E2` | Steam 1.05 `sub_408550` 调用 `sub_404C80(lpString)`，将拼出的 `begin.dhp` 提交给状态机 | IDA 静态分析 |

Hook 边界：当前只拦截 `sub_50A8E0` 播放包装。若路径尾部匹配 `begin.dhp` 且路径目录中包含 `dhp` 或 `bdh`，返回未播放，让原状态机清空该 CString 并自然进入后续状态。不拦截 `end.dhp`、其它剧情视频或非 `dhp`/`bdh` 目录下的同名文件。1.05 非 Steam 已动态确认 `dhp\begin.dhp`；Steam 1.05 已验证配合 `ComeOn.dll` 播放链 Hook 可跳过动画并正常进入游戏画面。不要在 Steam 1.05 上 Hook `sub_404C80` 阻止路径入队，该范围会破坏后续状态导致卡住。

### Steam `ComeOn.dll`

IDA 默认基址为 `0x10000000`。

| 函数/地址 | RVA | 说明 | 来源 |
| --- | --- | --- | --- |
| `StartAddress` / `0x10003F20` | `0x3F20` | DirectShow/COM 播放线程；拼接 `%s\bdh\%s\begin.dhp`，调用 `IGraphBuilder::RenderFile`，播放完成后关闭 `VideoWindow` | IDA 静态分析 |
| `sub_100041F0` / `0x100041F0` | `0x41F0` | 视频窗口线程；注册 `VideoWindow`，创建隐藏窗口，创建 `StartAddress`，消息循环结束后写状态 3；plugK Steam 1.05 跳过开场动画 Hook 识别目标 | IDA 静态分析 |
| `0x1000426A` | `0x426A` | `sub_100041F0` 中压入 `StartAddress` 作为 `CreateThread lpStartAddress` | IDA 静态分析 |
| `sub_100042E0` / `0x100042E0` | `0x42E0` | `CreateFileW` 包装；首次 `.ini` 路径触发 Steam 初始化、创建 `sub_100041F0` 并等待状态 3 | IDA 静态分析 |
| `0x100043C5` | `0x43C5` | `sub_100042E0` 中压入 `sub_100041F0` 作为 `CreateThread lpStartAddress` | IDA 静态分析 |
| `0x100043CE` | `0x43CE` | `sub_100042E0` 调用 `CreateThread` 创建视频窗口线程 | IDA 静态分析 |
| `dword_1001EE68` / `0x1001EE68` | `0x1EE68` | Steam 初始化/视频窗口状态；`sub_100042E0` 等待其变成 3 | IDA 静态分析 |
| `WindowName` / `0x100152A4` | `0x152A4` | 视频窗口标题宽字符串 | IDA 静态分析 |
| `aSBdhSBeginDhp` / `0x100152B0` | `0x152B0` | 宽字符串路径格式 `%s\bdh\%s\begin.dhp` | IDA 静态分析 |
| `ClassName` / `0x100152D8` | `0x152D8` | 宽字符串 `VideoWindow` | IDA 静态分析 |

Hook 边界：Steam 1.05 的开场动画来自 `ComeOn.dll`，不要只限定为 `launcher.exe` 阶段。`PlugKLauncherHook.dll` 在 `launcher.exe` 内等待或同步拦截 `ComeOn.dll` 加载，patch `ComeOn.dll` 的 `CreateThread` IAT；`PlugK.dll` 在 1.05 `ComeOn.exe` 内也会检查/等待本进程加载的 `ComeOn.dll` 并 patch 同一 IAT。保留 `sub_100041F0` 视频窗口线程和结尾清理，仅当 `lpStartAddress == StartAddress` 时跳过内部 DirectShow 播放线程，并向全局 `hWnd` 投递 `WM_CLOSE`，让 `sub_100041F0` 按原流程写 `dword_1001EE68 = 3`。

## 分辨率与显示模式

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_4043E0` / `0x004043E0` | 显示控制对象主循环更新；比较对象 `+8` 待切换模式与 `+4` 当前模式，不同时调用 `sub_404D30(this, pending_mode, 0)` | IDA 静态分析 |
| `sub_404D30` / `0x00404D30` | DirectDraw 显示模式切换入口；原版模式 `4/5/6` 分别设置 `640 x 480`、`800 x 600`、`1024 x 768` | IDA 静态分析 |
| `sub_4F9410` / `0x004F9410` | 图形包装对象构造；调用 `DirectDrawCreateEx` 取得 `IDirectDraw7` | IDA 静态分析 |
| `sub_4FA3F0` / `0x004FA3F0` | 图形包装对象虚表 `+0x5C`；依次调用 `SetCooperativeLevel`、`SetDisplayMode`，原版协作标志为 `0x11/0x13` | IDA 静态分析 |
| `sub_4F9910` / `0x004F9910` | 创建 caps `0x2200`（`PRIMARYSURFACE | 3DDEVICE`）的原版全屏主表面并保存到 `dword_56D85C` | IDA 静态分析 |
| `sub_441FC0` / `0x00441FC0` | 取得 `IDirect3D7`，以 `dword_56D85C` 调用 `CreateDevice` | IDA 静态分析 |
| `0x00441DD2` | `sub_441FC0` 返回失败后显示 `3D Func Error` 的唯一边界 | IDA 静态分析、1.05 窗口化实机验证 |
| `sub_442160` / `0x00442160` | 设置 Direct3D7 渲染目标并调用 `BeginScene` | IDA 静态分析 |
| `sub_4421C0` / `0x004421C0` | `IDirect3DDevice7::EndScene` 包装 | IDA 静态分析 |
| `sub_4FAEE0` / `0x004FAEE0` | 通过 `dword_56D85C` 的 `IDirectDrawSurface7::Blt` 写入 2D 内容 | IDA 静态分析 |
| `dword_56D858` / `0x0056D858` | 全局 `IDirectDraw7` 指针 | IDA 静态分析 |
| `dword_56D85C` / `0x0056D85C` | 原版全屏主表面，同时作为 Direct3D7 设备目标 | IDA 静态分析 |
| `dword_54B6CC` / `0x0054B6CC` | 全局 `IDirect3DDevice7` 指针 | IDA 静态分析 |
| `0x00548398` | 全局显示控制对象基址；IDA 当前误标为 `hdc` | IDA 静态分析 |
| `dword_54839C` / `0x0054839C` | 显示控制对象 `+4`，当前显示模式；`sub_404D30` 开始切换时写入 | IDA 静态分析 |
| `dword_5483A0` / `0x005483A0` | 显示控制对象 `+8`，待切换显示模式；设置界面与调试快捷键都会写入 | IDA 静态分析 |
| `0x00404D85` / `0x00404D8F` | 模式 `6` 的原版宽高常量 `1024/768`；plugK 自定义分辨率启用时由 `resolution.c` 替换 | IDA 静态分析、plugK 实现 |
| `aSetIni` / `0x00545BF8` | 字符串 `set.ini`；读写函数拼接 `sub_4EB8F0()` 返回的目录路径 | IDA 静态分析 |
| `sub_4A5770` / `0x004A5770` | `set.ini` 加载（Load）：拼路径 → `sub_4D3330` 打开 → `sub_4D3640("?")` 循环解析 6 个 `?` 分隔 token；第 1 个传给 `sub_4A5970`（分辨率枚举），第 2 个传给 `sub_4A59F0`，第 3~6 个为其它设置（`0x4B35D0(171/172/173)`、`dword_55788C`） | IDA 静态分析 |
| `sub_4A5610` / `0x004A5610` | `set.ini` 保存（Save）：`sub_4D3410` 打开后写回 `dword_548618`、`dword_5485D8`、`sub_472E70/472E60` 等 6 个值 | IDA 静态分析 |
| `sub_4A5970` / `0x004A5970` | 分辨率枚举唯一 setter（`__stdcall(int)`）：`dword_548618 = a1`，并按 `a1==4` 切换 `sub_4B5790` 调用；调用来源：Load 首 token（`0x4A582F`）、设置菜单 `sub_4A5B50` case `0xAE→4` / `0xAF→5`（菜单无 `1024x768` 选项）、`sub_4A6030` 应用回写；plugK 自定义分辨率启用时 `resolution.c` Hook 此处强制参数为 6 | IDA 静态分析、plugK 实现 |
| `dword_548618` / `0x00548618` | 全局分辨率枚举（`4/5/6`）；经显示控制对象 `+8`（`dword_5483A0`）由主循环触发 `sub_404D30` | IDA 静态分析 |

### 调试模式 `Shift+F2` 切换点

这些地址都位于 `sub_4748C0` 的 `dword_548644 != 0` 分支内：

| 地址 | 说明 | 来源 |
| --- | --- | --- |
| `0x00474C5C` | 检查当前帧 `VK_SHIFT` 状态 `byte_5489D8` | IDA 静态分析 |
| `0x00474CCB` | 检查当前帧 `VK_F2` 状态 `byte_548A39`，并结合 `byte_548B39` 只接受新按下边沿 | IDA 静态分析 |
| `0x00474CEE` | 当前屏幕宽度为 `800` 时写 `dword_5483A0 = 6` | IDA 静态分析 |
| `0x00474D08` | 当前屏幕宽度为 `640` 时写 `dword_5483A0 = 5` | IDA 静态分析 |
| `0x00474D26` | 当前屏幕宽度为 `1024` 时写 `dword_5483A0 = 4` | IDA 静态分析 |

详细激活方式、模式循环、`set.ini` 绕过边界和 plugK 自定义分辨率交互见 [1.05 调试模式](../systems/debug_mode_105.md#shiftf2-分辨率切换)。

## 调试模式相关函数

| 函数/地址 | 说明 |
| --- | --- |
| `sub_4C0670` | 文本输入提交后的隐藏调试入口；识别 `` `Tianxiawudi`` 并设置 `dword_548644` |
| `sub_458410` | 调试命令/脚本动作解释器；命令名大小写不敏感，支持用 `*` 分隔多条命令 |
| `sub_457860` | 条件判断类命令解析器，包含 `IsFlag`、`IsTimer`、`IsDead` 等脚本条件 |
| `sub_4748C0` | 调试开关开启后的部分运行时快捷键逻辑 |
| `sub_4756D0` | 调试开关开启后的主要 Ctrl 组合快捷键逻辑 |
| `sub_4C42B0` | 调试开关相关的 UI/面板快捷键逻辑 |

详细命令名和快捷键整理见 [1.05 调试模式](../systems/debug_mode_105.md)。

## 掉落相关函数

| 函数/地址 | 说明 | 对应关系 |
| --- | --- | --- |
| `sub_423580` | 连招得分计算函数 | 2.01 待确认 |
| `sub_423FB0` | 连招二次掉落触发逻辑，得分大于 20 时调用 `sub_424180(this, 1, score)` | 2.01 待确认 |
| `sub_424180` | 掉落入口，普通击败传入 bonus 0，连招二次掉落传入得分 | 2.01 待确认 |
| `sub_452420` | `RandProp` 驱动的掉落主流程：金钱判定、物品判定、数量与创建 | 2.01 `sub_45E5A0` |
| `sub_452390` | 按 `RandProp` 的 1..9 权重抽取等级/品级 | 2.01 待确认 |
| `sub_452200` | 根据等级/类型/难度从物品表选具体物品 ID | 2.01 `sub_45E390` |
| `sub_452730` | 地面物品拾取入口；Z 分支和部分接触/脚本路径都会调用，内部转入 `sub_47EEF0` | 2.01 `sub_45E8B0` |
| `sub_48B4F0` | 创建并投放地面物品 | 2.01 `sub_49AB70` |

## 自动拾取候选地址

1.05 静态对照确认原版 Z 拾取路径与 2.01 同构：

```text
sub_41C270(this, 22, ...)
    sub_44E460(this+0x2C, this+0x30, 0xC0)
    for each result:
        sub_452730(groundItem, this)
        sub_44E510()
```

当前候选地址：

| 地址 | 说明 | 状态 |
| --- | --- | --- |
| `0x0041C270` | 行为入口，`a2 == 22` 为 Z 拾取分支 | IDA 静态确认，动态验证 |
| `0x0041C872` | Z 分支起点，调用 `sub_44E460(..., 0xC0)` | IDA 静态确认，动态验证 |
| `0x0041C892` | `sub_452730` 返回地址，限定 Z 调用来源 | 动态验证 |
| `0x00452730` | 地面物品拾取入口 | IDA 静态确认，动态验证 |
| `0x00558E4C` | 地面/地图管理器指针 | 动态验证 |
| `0x00558E50` | 6 字节对象槽表指针 | 动态验证 |
| `0x00528CD4` | 当前 Z 行为对象 vtable，`this+4 == *0x00558E4C` | 动态验证 |
| `0x005288F8` | 实际地面物品 vtable；`vtable+0x20 == 0x004128E0` 为拾取前状态处理 | 动态验证 |

## 地面物品名称显示

| 函数/地址 | 说明 | 来源 |
| --- | --- | --- |
| `sub_412B30` / `0x00412B30` | 地面物品更新函数，plugK 长显名称 hook 目标 | 静态分析 |
| `sub_4127D0` / `0x004127D0` | 地面物品名称显示函数，plugK 长显名称直接调用目标 | 静态分析、动态调试 |
| `sub_4CC4B0` / `0x004CC4B0` | 创建文字节点函数；`0x004128AE` 为 `sub_4127D0` 内部调用点 | 动态调试 |
| `sub_4CC440` / `0x004CC440` | 地面浮动文字管理器绘制函数，遍历 `this+0xC4` 文字节点链表 | 静态分析 |
| `sub_4B1DF0` / `0x004B1DF0` | 通用文字绘制包装；参数控制是否绘制背景 | 静态分析 |
| `sub_4B25A0` / `0x004B25A0` | 文字输出函数；调用 `SetTextColor` / `TextOutA`，支持 `$G/$Y/$B/$R/$W/$H` 控制码 | 静态分析、动态调试 |
| `0x0041289C` | `push 0`，样式参数，未观察到会改变文字颜色 | 动态调试 |
| `0x0041289E` | `push 0`，样式参数，未观察到会改变文字颜色 | 动态调试 |
| `0x004128A3` | `push 0`，写入文字节点 `+0x0C`；动态改为 `push 1` 后去除名称背景，改为 `push 2` 会恢复背景/边框 | 动态调试、plugK 1.05 实现 |
| `dword_55C1F8` / `0x0055C1F8` | 地面浮动文字管理器指针；对象 `+0x60` 为默认文字颜色 `COLORREF` | 动态调试 |
| `0x00545C48` | 文本颜色控制码使用的一组 `COLORREF` 颜色值，附近字符串为 `$H/$W/$R/$B/$Y/$G` | 动态调试 |

## `sub_452420` 关键字段访问

`sub_452420` 的第一个参数会按 `RandProp.角色ID` 查询运行时表。已确认字段访问：

| 字段号 | RandProp 列 | 用途 |
| ---: | --- | --- |
| 2 | 掉宝机率 % | 物品掉落判定 |
| 3 | 掉宝数量min | 物品数量下限 |
| 4 | 掉宝数量max | 物品数量上限 |
| 5 | 掉钱机率 % | 金钱掉落判定 |
| 6 | 掉钱数量min | 金钱数量下限 |
| 7 | 掉钱数量max | 金钱数量上限 |
| 8..16 | 1..9 | 品质/掉落档位权重 |

## 掉落选物关键地址

`sub_452200(a1, a2)` 中，只有 `a2 == -1` 或 `a2 == 0xFFFF` 时才会随机决定物品类型。

| 地址 | 指令/行为 | 说明 |
| --- | --- | --- |
| `0x00452271` | `mov edi, 1Eh` | 生成 Type 30 |
| `0x004522E6` | 设置 Type 10 或 Type 20 | 未命中 Type 30 后的分支 |

## `sub_452420` 内部调用 `sub_452200` 的返回地址

这些返回地址用于把 hook 限定在敌人随机掉落路径内：

- `0x00452630`
- `0x00452641`
- `0x00452652`
- `0x00452663`

## Hook 注意事项

- 不要把 `sub_452200` 的所有调用都当作敌人随机掉落。
- 敌人拾取到的已有物品吐出逻辑不应归入随机掉落倾向优化。
- 修改具体物品 ID 时，优先在 `sub_452420` 内部调用返回点做边界判断。
